# Python
__pycache__/
*.pyc
*.pyo
.venv/
*.egg-info/

# Audio files (never commit) — except dependency reference WAV
*.raw
*.wav
!dependencies/cochran-reference-24k.wav

# TTS temp output
cochran_tmp/

# Case context files (gitignored — never commit case details)
matters/*.py
!matters/test_context.py
!matters/case_context.example.py

# Transcripts (gitignored — never commit call recordings)
transcripts/*.txt

# Large model files — excluded from git (see dependencies/README.md)
dependencies/kokoro-model/kokoro-v1_0.pth

# Driver files — available from vendor
dependencies/VBCABLE_Driver_Pack45/

# Environment files
.env

# OS files
.DS_Store
Thumbs.db